# The main pipeline for finpay_lakehouse

resources:
  pipelines:
    finpay_lakehouse_etl:
      name: finpay_lakehouse_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/finpay_lakehouse_etl"

      libraries:
        - glob:
            include: ../src/finpay_lakehouse_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
